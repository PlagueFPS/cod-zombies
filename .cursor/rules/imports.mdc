---
alwaysApply: true
---

## Convention
- Always use the import alias `@/*` for any interal imports
- Always use `PascalCase` for default import names like:
  ```typescript
  import SomeImage from "@/some-path"
  import SomeComponent from "@/some-path"
  ```