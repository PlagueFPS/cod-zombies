import ZombieTooltip from "@/components/zombie-tooltip"
import { MarkdownLink } from "@/components/markdown-link"

In <MarkdownLink to="/main-quests/$gameId/$mapId" params={{ gameId: "black-ops-3", mapId: "der-eisendrache" }} text={"Der Eisendrache"} />, these skeletons are truly no different than the regular <ZombieTooltip zombieKey='zombie' /> and can be treated as such, 
maintaining distance and mowing them down, with headshots providing extra damage. In <MarkdownLink to="/main-quests/$gameId/$mapId" params={{ gameId: "black-ops-4", mapId: "ancient-evil" }} text={"Ancient Evil"} />, aiming for the head should be your only focus 
to make sure these enemies stay down for good.